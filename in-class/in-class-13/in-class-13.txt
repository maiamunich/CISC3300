CISC 3300 F24
In class assignment 13
1. Variable names start with a $ in PHP
2. associative behave a bit like objects in JS or dictionaries in Python
3. A template literal in PHP would be “Welcome $name” or “Welcome {$name}”
4. PHP is loosely typed - we don’t set types for variables, they are Type juggling
5. The include (or require)  statement takes all the text/code/markup that exists in the specified file and copies it into the file that uses the include statement.
6. Create a PHP application that when accessed via its domain in a browser(localhost) an
HTML page is returned. On that HTML page include a button that when clicked makes
use of jQuery code to request and show data from the PHP backend.